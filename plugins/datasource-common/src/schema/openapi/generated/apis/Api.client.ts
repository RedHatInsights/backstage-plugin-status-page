//

// ******************************************************************
// * THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY. *
// ******************************************************************
import { DiscoveryApi } from '../types/discovery';
import { FetchApi } from '../types/fetch';
import crossFetch from 'cross-fetch';
import { pluginId } from '../pluginId';
import * as parser from 'uri-template';
import { CreateDatasource } from '../models/CreateDatasource.model';
import { Datasource } from '../models/Datasource.model';

/**
 * Wraps the Response type to convey a type on the json call.
 *
 * @public
 */
export type TypedResponse<T> = Omit<Response, 'json'> & {
  json: () => Promise<T>;
};

/**
 * Options you can pass into a request for additional information.
 *
 * @public
 */
export interface RequestOptions {
  token?: string;
}
/**
 * @public
 */
export type AddDatasource = {
  body: CreateDatasource;
};
/**
 * @public
 */
export type DeleteDatasource = {
  path: {
    namespace: string;
    name: string;
  };
};
/**
 * @public
 */
export type GetDatasource = {
  path: {
    namespace: string;
    name: string;
  };
};
/**
 * @public
 */
export type ListDatasources = {};
/**
 * @public
 */
export type UpdateDatasource = {
  path: {
    namespace: string;
    name: string;
  };
  body: CreateDatasource;
};

/**
 * @public
 */
export class DefaultApiClient {
  private readonly discoveryApi: DiscoveryApi;
  private readonly fetchApi: FetchApi;

  constructor(options: {
    discoveryApi: { getBaseUrl(pluginId: string): Promise<string> };
    fetchApi?: { fetch: typeof fetch };
  }) {
    this.discoveryApi = options.discoveryApi;
    this.fetchApi = options.fetchApi || { fetch: crossFetch };
  }

  /**
   * Create a new datasource
   * @param createDatasource -
   */
  public async addDatasource(
    // @ts-ignore
    request: AddDatasource,
    options?: RequestOptions,
  ): Promise<TypedResponse<Datasource>> {
    const baseUrl = await this.discoveryApi.getBaseUrl(pluginId);

    const uriTemplate = `/`;

    const uri = parser.parse(uriTemplate).expand({});

    return await this.fetchApi.fetch(`${baseUrl}${uri}`, {
      headers: {
        'Content-Type': 'application/json',
        ...(options?.token && { Authorization: `Bearer ${options?.token}` }),
      },
      method: 'POST',
      body: JSON.stringify(request.body),
    });
  }

  /**
   * Delete a datasource
   * @param namespace -
   * @param name -
   */
  public async deleteDatasource(
    // @ts-ignore
    request: DeleteDatasource,
    options?: RequestOptions,
  ): Promise<TypedResponse<void>> {
    const baseUrl = await this.discoveryApi.getBaseUrl(pluginId);

    const uriTemplate = `/{namespace}/{name}`;

    const uri = parser.parse(uriTemplate).expand({
      namespace: request.path.namespace,
      name: request.path.name,
    });

    return await this.fetchApi.fetch(`${baseUrl}${uri}`, {
      headers: {
        'Content-Type': 'application/json',
        ...(options?.token && { Authorization: `Bearer ${options?.token}` }),
      },
      method: 'DELETE',
    });
  }

  /**
   * Get a datasource by ID
   * @param namespace -
   * @param name -
   */
  public async getDatasource(
    // @ts-ignore
    request: GetDatasource,
    options?: RequestOptions,
  ): Promise<TypedResponse<Datasource>> {
    const baseUrl = await this.discoveryApi.getBaseUrl(pluginId);

    const uriTemplate = `/{namespace}/{name}`;

    const uri = parser.parse(uriTemplate).expand({
      namespace: request.path.namespace,
      name: request.path.name,
    });

    return await this.fetchApi.fetch(`${baseUrl}${uri}`, {
      headers: {
        'Content-Type': 'application/json',
        ...(options?.token && { Authorization: `Bearer ${options?.token}` }),
      },
      method: 'GET',
    });
  }

  /**
   * List all datasources
   */
  public async listDatasources(
    // @ts-ignore
    request: ListDatasources,
    options?: RequestOptions,
  ): Promise<TypedResponse<Array<Datasource>>> {
    const baseUrl = await this.discoveryApi.getBaseUrl(pluginId);

    const uriTemplate = `/`;

    const uri = parser.parse(uriTemplate).expand({});

    return await this.fetchApi.fetch(`${baseUrl}${uri}`, {
      headers: {
        'Content-Type': 'application/json',
        ...(options?.token && { Authorization: `Bearer ${options?.token}` }),
      },
      method: 'GET',
    });
  }

  /**
   * Update an existing datasource
   * @param namespace -
   * @param name -
   * @param createDatasource -
   */
  public async updateDatasource(
    // @ts-ignore
    request: UpdateDatasource,
    options?: RequestOptions,
  ): Promise<TypedResponse<Datasource>> {
    const baseUrl = await this.discoveryApi.getBaseUrl(pluginId);

    const uriTemplate = `/{namespace}/{name}`;

    const uri = parser.parse(uriTemplate).expand({
      namespace: request.path.namespace,
      name: request.path.name,
    });

    return await this.fetchApi.fetch(`${baseUrl}${uri}`, {
      headers: {
        'Content-Type': 'application/json',
        ...(options?.token && { Authorization: `Bearer ${options?.token}` }),
      },
      method: 'PUT',
      body: JSON.stringify(request.body),
    });
  }
}
